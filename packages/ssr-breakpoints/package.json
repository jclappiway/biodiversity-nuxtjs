{
  "name": "@biodiversity-nuxtjs/ssr-breakpoints",
  "version": "0.0.0",
  "description": "Detect device and infer screen resolution on server or device and screen resolution breaks on client",
  "license": "MIT",
  "contributors": [
    {
      "name": "Convention on Biological Diversity <it@cbd.int>"
    }
  ],
  "main": "lib/module.js",
  "repository": "https://github.com/https://github.com/scbd/biodiversity-nuxtjs.git",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "lint": "eslint lib test",
    "test": "npm run lint && jest",
    "release": "standard-version && git push --follow-tags && npm publish"
  },
  "eslintIgnore": [
    "lib/templates/*.*"
  ],
  "files": [
    "lib"
  ],
  "jest": {
    "testEnvironment": "node",
    "coverageDirectory": "./coverage/",
    "collectCoverage": true,
    "collectCoverageFrom": [
      "lib",
      "test"
    ]
  },
  "dependencies": {

  },
  "devDependencies": {
    "nuxt-module-builder": "latest"
  }
}
